/*
 ** 📁 plan.txt|ToDo.txt
 ** lastUpdatedAt:[{ "date": "10/09/2025", "by": ["BomBa"], "comment": "ملف خاص لإدارة المشروع و اخر التطورات" }]
 */

## مشروع (Web) - Vite Vanilla JS:
  ```bash
    $ rm -rf ./*                      # (إختياري) امر حذف جميع ما بداخل الملف الحالي
    $ rm -rf ./* .[^.] .??*           # أو مع إظهار الملفات المخفية (مثل .gitignore, .env …):

    # 1) إنشاء المشروع بداخل المجلد الحالي مع إتباع القواعد الموصي بها
      $ npx create-next-app@latest ./ --ts --eslint --src-dir --app --import-alias "@/*" --no-tailwind --no-cache --turbopack

      $ pnpm create vite .
      $ yarn create vite .
      $ yarn dev
      
    # 2) تثبيت أهم المكاتب التي سنعتمد عليها
      # 🚀 1. أمر تثبيت المكتبات الأساسية
      yarn add sass 
        # 💡 شرح المكتبات:
        # sass: لتفعيل استخدام SCSS بدلاً من TailwindCSS.
      # 🚀 2. مكتبات التطوير (devDependencies)
      yarn add -D eslint-plugin-jsx-a11y javascript-obfuscator
        # 💡 شرح المكتبات:
        # eslint-plugin-jsx-a11y: إضافة لـ ESLint تركز على قواعد الوصولية في JSX/React، وتساعدك على اكتشاف الأخطاء التي قد تمنع ذوي الاحتياجات الخاصة من استخدام التطبيق.
        # javascript-obfuscator: يقوم بتشفير وتضليل الكود الخاص بالواجهة الأمامية (JS) في مرحلة الـ Build لزيادة صعوبة قراءته.

    # 3) إنشاء البنية الاولية للمشروع
      # 1. حذف ملف [public, src] و ملفات المشروع الإفترادي:
      rm -rf ./src ./public
      # 2. إعادة إنشاء الهيكل المفضل
        # إنشاء المجلدات
        mkdir -p src/{assets/{icons,images},app/[locale],components/{Layout,Global},hooks,i18n/{locales/{ar,en},config},routes,services,store/{slices/auth/act,thunks},styles/{lib,utils,components,layouts},types,utils}
        # إنشاء الملفات
        touch src/{assets/{icons.svg,images.svg},app/{[locale]/{layout.tsx,page.tsx},head.tsx},components/{Layout/{_header.scss,_footer.scss},Global/{_BUI_buttons.scss,_BUI_forms.scss}},i18n/{locales/{ar/index.ts,en/index.ts},config.ts,generate-json-locales.ts},routes/{AppRouter.tsx,index.ts},store/{slices/{auth/{act/actAuth.ts,authSlice.ts},settingsSlice.ts},index.ts},styles/{main.scss,lib/_normalize.scss,utils/{_index.scss,_var.scss,_fun.scss,_mixin.scss}},types/{auth.types.ts,global.types.ts,router.types.ts,shared.types.ts,index.ts},utils/{alias-Image-Icons.ts,alias.ts,B_lips.ts}}

    # 4) قم بتنسيق الكود داخل الملفات
  ```

---------- ---------- ---------- ---------- ---------- ---------- ----------

## Git|GitHub:
  ### أوامر الإعدادات العامة:
    git status                                              # 🔹 حالة المشروع, بيعرض الملفات المعدلة واللي اتضافت أو لسه مش متتبعة
    git init                                                # 🔹 تهيئة (Initialize) مستودع جديد, بيعمل مستودع Git جديد في المجلد الحالي
    git add file.txt                                        # 🔹 إضافة الملفات للتتبع (Staging)
    git add .                                               # أو كل الملفات:
    git commit -m "The Message"                             # 🔹 حفظ التغييرات (Commit), بيسجل التغييرات اللي اتعملت في الـ staging area
    git log                                                 # 🔹 عرض السجل (History), بيعرض الكوميتات اللي حصلت
  ### أوامر إعدادات الفرع (branch):
    git branch new-branch                                   # 🔹 إنشاء فرع جديد
    git checkout new-branch                                 # 🔹 الانتقال لفرع
    git switch new-branch                                   # 🔹 الانتقال لفرع في الإصدارات الحديثة
    git checkout -b new-branch                              # 🔹 إنشاء + الانتقال لفرع جديد
    git switch -c new-branch                                # 🔹 إنشاء + الانتقال لفرع جديد في الإصدارات الحديثة
    git merge new-branch                                    # 🔹 دمج فرع (Merge), بيدمج التغييرات من الفرع الجديد مع الفرع الحالي
    git push -u origin new-branch                           # 🔹 رفع فرع لأول مرة
    git push                                                # 🔹 رفع فرع فيما بعد
    git branch                                              # 🔹 إظهار الفروع, بيعرض الفروع المحلية
    git branch -a                                           # 🔹 إظهار الفروع, بيعرض الفروع المحلية + اللي على GitHub
    git branch -d branch-name                               # 🔹 حذف فرع
  ### أوامر إعدادات الرفع و التنزيل (GitHub):
    git remote add origin https://github.com/user/repo.git  # 🔹 ربط المشروع بمستودع GitHub
    git push origin main                                    # 🔹 رفع التغييرات (Push), بيرفع التغييرات لفرع main في GitHub
    git pull origin main                                    # 🔹 جلب التغييرات (Pull), بينزل آخر التغييرات من GitHub ويدمجها مع الكود عندك
    git clone https://github.com/user/repo.git              # 🔹 استنساخ مستودع (Clone), بينزل نسخة كاملة من المستودع

  ### Commit:
  V:0.0.1
    - المهمة: إزالة الملفات الافتراضية وإعداد هيكل مشروع جديد
    - النمط: إضافة SCSS، ومزيجات، وأنماط أساسية.
    - الوثائق: إضافة دليل البدء السريع وقائمة المهام.
  --- --- --- --- --- --- --- --- --- --- --- --- --- --- ---

---------- ---------- ---------- ---------- ---------- ---------- ----------

## (Notes) ملحوظات لتسهيل العمل:
  // 0.rfc
  import React from 'react'

  export default function page() {
    return (
      <div>page</div>
    )
  }

  // 1.rafc
  import React from 'react'

  export const page = () => {
    return (
      <div>page</div>
    )
  }

  // 2.rafce
  import React from 'react'

  const page = () => {
    return (
      <div>page</div>
    )
  }

  export default page

  // 3.rafce
  import React from 'react'
  import PropTypes from 'prop-types'

  const page = props => {
    return (
      <div>page</div>
    )
  }

  page.propTypes = {}

  export default page

  // 4.rcc
  import React, { Component } from 'react'

  export default class page extends Component {
    render() {
      return (
        <div>page</div>
      )
    }
  }

---------- ---------- ---------- ---------- ---------- ---------- ----------

## (Plan && ToDo) خطة العمل و المهام العامة:

### 🟢TODO:
  - [X] - step1 (تم إنشاء ملفات المشروع: البنية الهيكلية الاساسية).
  - [ ] - step2 (جاري التحسينات).
        - [ ] - تحسين انماط SCSS و إتباع اسلوب موحد.
        - [ ] - تحسين مكتبة الاوضاع (./src/utils/BThemes.js).
              - [ ] تفعيل حفظ الوضع المختار في التخزين المحلي
        - [ ] - تحسين مكتبة النوافذ (./src/utils/BModal.js).
              - [ ] تحسين الشكل.
        - [ ] - يتم تحسين ملف (README.md) بشكل مؤقت.
  - [ ] - .


### 📌 نظام إدارة المهام (Todo)
    ------ ------ ------ ------
  🟢 مهام صغيرة (Quick Tasks)
  - [ ] Fix Bug في تسجيل الدخول.
  - [ ] تعديل CSS للـ Navbar.
  - [ ] إضافة Validation لحقل البريد.
  - [ ] تحسين رسالة الخطأ عند 404.
    ------ ------ ------ ------
  🟡 مهام متوسطة (Medium Tasks)
  - [ ] بناء API لإدارة المستخدمين (CRUD).
  - [ ] إضافة Middleware للأمان (Helmet + Rate Limiter).
  - [ ] تصميم صفحة Dashboard مع البيانات الأساسية.
  - [ ] ربط المشروع بقاعدة بيانات SQLite/Postgres.
    ------ ------ ------ ------
  🔴 مهام كبيرة (Big Tasks)
  - [ ] إنشاء نظام إدارة مخزن/مبيعات كامل.
  - [ ] مشروع To-Do List مع Auth + i18n + Dark Mode.
  - [ ] إعادة هيكلة Backend لمشروع قائم (Refactor + Security).
  - [ ] بناء تطبيق موبايل (React Native / Flutter).

### ⏱️ سجل الوقت (Daily Log)
    ------ ------ ------ ------
  📅 اليوم: _________
  🕒 وقت البداية: ____
  🕒 وقت النهاية: ____
  ✅ الإنجاز اليومي:
  - _________
  - _________
  ⚡ خطة الغد:
  - _________
  - _________

### 📊 تقييم أسبوعي (Weekly Review)
    ------ ------ ------ ------
  📅 الأسبوع: ______________

  ✅ إنجازاتي:
  - _________
  - _________

  ⏳ ضيعت وقت في:
  - _________
  - _________

  🎯 خطة الأسبوع القادم:
  - _________
  - _________
